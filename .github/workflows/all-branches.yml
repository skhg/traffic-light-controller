name: All Branches
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install ESlint
        run: |
          yarn add eslint
      - name: Run ESLint checks
        run: |
          yarn run eslint ./webapp/*.js
      - name: Cleanup
        run: |
          rm -rf node_modules
          rm package.json
          rm yarn.lock